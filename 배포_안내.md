# 옆커폰 매장관리 — Cloudflare 배포 안내 (GitHub 방식)

## 왜 GitHub를 거쳐야 하나요?

Cloudflare 대시보드에 파일을 **드래그 앤 드롭으로 올리는 방식은 서버 기능(functions 폴더)이 작동하지 않습니다.**
이건 Cloudflare 공식 문서에 명시된 제약이에요. 화면은 뜨지만 저장이 안 됩니다.

그래서 **GitHub에 파일을 올려두고, Cloudflare가 그 GitHub를 바라보게** 연결해야 해요.
명령어(터미널) 입력은 전혀 필요 없고, 전부 웹 화면에서 마우스로 됩니다.

**보너스**: 이 방식으로 하면 나중에 수정할 때 GitHub에서 파일만 교체하면 사이트가 자동으로 업데이트돼요.

---

## 준비물

- 이 폴더 안의 파일들 (`index.html`, `functions/api/kv.js`)
- 이메일 주소 (GitHub 가입용)
- 이미 만들어 둔 Cloudflare 계정 + KV 네임스페이스 (`yeopcheorpon-store`) — 그대로 재사용합니다

---

## 1단계 — GitHub 가입 (약 3분)

1. https://github.com/signup 접속
2. 이메일 → 비밀번호 → 사용자 이름 순서로 입력
3. 이메일로 온 인증 코드 입력
4. 무료 플랜(Free) 선택

---

## 2단계 — 저장소(Repository) 만들기

"저장소"는 그냥 파일을 보관하는 폴더라고 생각하시면 돼요.

1. https://github.com/new 접속
2. **Repository name** 칸에 입력: `yeopcheorpon-store`
3. **Public**(공개) 또는 **Private**(비공개) 선택
   - **Private 추천** — 코드가 아무에게나 보이지 않아요. Cloudflare 연결은 Private이어도 잘 됩니다.
4. 아래 **`Create repository`** 초록 버튼 클릭

---

## 3단계 — 파일 올리기 ⭐ 폴더 구조가 중요해요

`functions/api/kv.js` 의 **폴더 구조가 그대로 유지되어야** 서버 기능이 작동해요.
GitHub 웹 화면에서 폴더째로 끌어다 놓으면 구조가 유지됩니다.

1. 방금 만든 저장소 화면에서 **`uploading an existing file`** 링크 클릭
   - 또는 **`Add file`** → **`Upload files`**
2. 압축을 푼 폴더 안에서 **`index.html` 파일과 `functions` 폴더를 함께 선택해서** 업로드 영역에 끌어다 놓기
   - ⚠️ zip 파일을 올리면 안 돼요. **압축을 먼저 푼 뒤** 안의 내용물을 올려주세요.
   - ⚠️ `functions` 폴더는 파일 하나씩 말고 **폴더째로** 끌어다 놓아야 구조가 유지돼요.
3. 업로드 목록에 이렇게 보이면 정상이에요:
   ```
   index.html
   functions/api/kv.js
   ```
4. 화면 아래 **`Commit changes`** 초록 버튼 클릭

✅ 저장소 화면에 `index.html` 과 `functions` 폴더가 보이면 성공이에요.
`functions` 폴더를 클릭해서 들어갔을 때 `api` → `kv.js` 순서로 들어있는지 꼭 확인해주세요.

---

## 4단계 — Cloudflare에서 새 프로젝트 만들기 (Git 연결)

⚠️ 기존에 만드신 `yeopcheorpon-stor` 프로젝트는 직접 업로드 방식이라 Git으로 바꿀 수 없어요.
**새 프로젝트를 하나 더 만들어야 합니다.** (기존 건 나중에 지우면 돼요)

1. https://dash.cloudflare.com/?to=/:account/workers-and-pages 접속
2. **`애플리케이션 생성`** 클릭
3. **`Pages`** 탭 선택
4. 이번엔 **`기존 Git 리포지토리 가져오기`** 쪽의 **`시작`** 클릭
   (지난번에 고른 "파일 끌어서 놓기"가 아니에요!)
5. **`GitHub 연결`** 클릭 → GitHub 로그인 → 권한 승인
   - "모든 저장소" 또는 방금 만든 `yeopcheorpon-store`만 선택해도 돼요
6. 목록에서 `yeopcheorpon-store` 저장소 선택 → **`설정 시작`**
7. 빌드 설정 화면이 나오면:

   | 항목 | 입력값 |
   |---|---|
   | 프레임워크 사전 설정 | **없음 (None)** |
   | 빌드 명령 | **비워두기** |
   | 빌드 출력 디렉터리 | **비워두기** (또는 `/`) |

   > 우리 프로젝트는 빌드가 필요 없는 순수 HTML이라 전부 비워두면 돼요.

8. **`저장 및 배포`** 클릭 → 1~2분 대기

---

## 5단계 — KV 연결 (새 프로젝트에 다시 해줘야 해요)

기존에 만든 KV 저장공간을 **새 프로젝트에** 연결합니다. KV는 새로 만들 필요 없어요.

1. 새로 만든 프로젝트 → **`설정`** 탭
2. **`바인딩`** 항목 → **`+ 추가`** 클릭
3. **`KV 네임스페이스`** 선택
4. 입력:

   | 칸 | 값 |
   |---|---|
   | 변수 이름 | **`STORE_KV`** |
   | KV 네임스페이스 | `yeopcheorpon-store` |

5. **저장**

---

## 6단계 — 재배포

Git 방식은 재배포가 쉬워요.

1. **`배포`** 탭 → 맨 위 배포의 **`···`** → **`배포 재시도`** 클릭
2. 메뉴가 없으면: GitHub 저장소에서 아무 파일이나 살짝 수정하고 Commit → 자동으로 다시 배포돼요

---

## 7단계 — 확인

1. `https://프로젝트이름.pages.dev` 접속
2. 매장 선택 → 문의량 `+` 눌러보기 → **에러 안 뜨면 성공!** 🎉
3. 휴대폰으로 같은 주소 열기 → 방금 입력한 숫자가 보이면 완벽하게 연동된 거예요
4. 관리자 분석 → 비밀번호 `2026` → 데이터 확인

---

## 8단계 — 링크 공유

`https://프로젝트이름.pages.dev` 를 점장님들 단톡방에 공유하세요.
설치 필요 없고, 휴대폰 홈 화면에 추가하면 앱처럼 쓸 수 있어요.

---

## 앞으로 수정하고 싶을 때

1. 저(Claude)에게 수정 요청 → 새 `index.html` 받기
2. GitHub 저장소에서 기존 `index.html` 클릭 → 연필(✏️) 아이콘 → 내용 교체 → Commit
   - 또는 `Add file` → `Upload files` 로 같은 이름 파일 덮어쓰기
3. **Cloudflare가 자동으로 감지해서 사이트를 업데이트해줍니다** (1~2분)

---

## 참고 · 주의사항

- **관리자 비밀번호(`2026`)는 화면단에서만 확인하는 방식이라 완벽한 보안은 아니에요.**
  코드를 열어보면 비밀번호가 보일 수 있어요. 내부 직원 실수 방지 용도로는 충분하지만,
  더 튼튼하게 하려면 서버 쪽에서도 확인하도록 보강할 수 있어요 (필요하시면 말씀해주세요).
- 비밀번호 변경: `index.html` 안에서 `ADMIN_PASSWORD` 검색 → 값 수정 → GitHub에 다시 올리기
- 기존 `yeopcheorpon-stor` 프로젝트는 새 사이트가 잘 되는 걸 확인한 뒤 삭제하셔도 돼요.
- 커스텀 도메인(예: `store.회사도메인.com`) 연결도 가능해요. 필요하면 도와드릴게요.
